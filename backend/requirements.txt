# =====================================================
# SkillForge AI Backend - Environment Configuration
# =====================================================
# This file documents the required environment variables
# Copy these to backend/.env and fill in your values
# =====================================================

# =====================================================
# REQUIRED VARIABLES
# =====================================================

# MongoDB Connection (REQUIRED)
# Local: mongodb://localhost:27017/skillforge-ai
# Atlas: mongodb+srv://<user>:<pass>@cluster.mongodb.net/skillforge-ai
MONGODB_URI=mongodb://localhost:27017/skillforge-ai

# JWT Secret (REQUIRED) - Change in production!
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=skillforge_jwt_secret_change_this_in_production_64chars_minimum
JWT_EXPIRES_IN=7d

# Frontend URL (REQUIRED)
CLIENT_URL=http://localhost:3000

# =====================================================
# OPTIONAL VARIABLES
# =====================================================

# Server Config
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# Google OAuth - https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth - https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# AI Providers (NOT REQUIRED - leave empty)
# App uses built-in mock responses for all AI features
GEMINI_API_KEYS=
GROQ_API_KEYS=
PERPLEXITY_API_KEYS=

# Optional overrides
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models
GEMINI_MODEL=gemini-1.5-flash
GROQ_API_URL=https://api.groq.com/openai/v1/chat/completions
GROQ_MODEL=llama-3.1-70b-versatile
PERPLEXITY_API_URL=https://api.perplexity.ai/chat/completions
PERPLEXITY_MODEL=sonar-pro

# Email - https://myaccount.google.com/apppasswords
# (Console logging used if not configured)
EMAIL_SERVICE_PROVIDER=gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USERNAME=
EMAIL_PASSWORD=

# CORS
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Admin Defaults (used by seed script)
ADMIN_EMAIL=admin@skillforge.ai
ADMIN_DEFAULT_PASSWORD=Admin@123

# Session
SESSION_SECRET=skillforge_session_secret

# =====================================================
# NPM DEPENDENCIES (for reference)
# =====================================================
# Run: npm install
#
# Production:
# - express@^4.18.2 - Web framework
# - mongoose@^8.0.3 - MongoDB ODM
# - bcryptjs@^2.4.3 - Password hashing
# - jsonwebtoken@^9.0.2 - JWT authentication
# - passport@^0.7.0 - Auth middleware
# - passport-google-oauth20@^2.0.0 - Google OAuth
# - passport-github2@^0.1.12 - GitHub OAuth
# - nodemailer@^6.9.7 - Email sending
# - node-cron@^3.0.3 - Scheduled tasks
# - axios@^1.6.2 - HTTP client for AI API
# - express-validator@^7.0.1 - Input validation
# - express-rate-limit@^7.1.5 - Rate limiting
# - helmet@^7.1.0 - Security headers
# - cors@^2.8.5 - CORS handling
# - morgan@^1.10.0 - HTTP logging
# - dotenv@^16.3.1 - Environment variables
#
# Dev Dependencies:
# - nodemon@^3.0.2 - Auto-restart server
# =====================================================

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload Configuration
MAX_FILE_SIZE_MB=10
UPLOAD_PATH=./uploads

# Session Configuration
SESSION_SECRET=your_session_secret_key
